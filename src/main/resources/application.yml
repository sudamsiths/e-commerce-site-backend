spring:
  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce_site?createDatabaseIfNotExist=true
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
      enabled: true

  file:
    upload-dir: uploads/products
